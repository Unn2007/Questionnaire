export const selectQuizes = (state) => state.quiz.items;
export const selectIsLoading = (state) => state.quiz.loading;
export const selectLastKey = (state) => state.quiz.lastKey;
export const selectPage = (state) => state.quiz.page;
export const selectTotal = (state) => state.quiz.total;
export const selectIsQuizInfo = (state) => state.quiz.isQuizInfo;
